export function AutoTerminal({
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  lines,
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  onComplete,
}: {
  lines: string[];
  onComplete: () => void;
}) {
  return <div></div>;
}
